{
  "name": "@monsave/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MonSave settlement automation worker — optional convenience, never a dependency",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@monsave/config": "workspace:*",
    "pino": "9.6.0",
    "viem": "2.23.2",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@types/node": "22.10.10",
    "tsx": "4.19.2",
    "typescript": "5.7.3"
  }
}
